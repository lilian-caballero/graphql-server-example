const types = require('./types');
const inputs = require('./inputs');
const mutations = require('./mutations');
const queries = require('./queries');

module.exports = [types, inputs, mutations, queries]